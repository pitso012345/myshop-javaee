A simple RESTful CRUD web service using Java EE 7 specifically JAX-RS, CDI, EJB, JPA and Bean Validation.
It also uses Arquillian, embedded Glassfish container and embedded Derby DB for integration testing.
